import { createTheme } from "@mui/material/styles";

export const Btn = createTheme({
  palette: {
    primary: {
      main: "#00000000",
      contrastText: "#fff",
    },
  },
});
export const GridBackground = createTheme({
  palette: {
    primary: {
      main: "#996220",
      contrastText: "#fff",
    },
  },
});
